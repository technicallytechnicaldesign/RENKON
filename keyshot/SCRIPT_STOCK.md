# Script Stock

Project ID: `ksrp-efbe4961`
Last updated: 2026-07-16 (bumped the material generator `AB04 → AB05`: **texture scaling is now PART-SIZE AWARE**. AB04 and earlier were not: no bounding-box query anywhere, and `Center On` (texture_space) was never set — so KeyShot defaulted to *Center On: Model* and every procedural texture mapped to the whole ~6700-unit MODEL, making feature sizes wildly wrong on a ~40 mm part; all texture Scales were hardcoded absolutes; and Spots' own tiling Scale was never set at all (the `["radius","size","scale"]` list matched Radius first → Scale stayed at the ~6700 default → giant blobs). AB05: (1) `resolve_part_size(opts, name_filter)` — operator-entered `part_size_mm` dialog field if > 0, else `measure_part_size` walks the scene geometry trying UNPROBED bbox APIs (getWorldBounds/getBounds/getBoundingBox, max extent across matched parts), else `DEFAULT_PART_SIZE_MM = 50`; captured into `spec['scale']` for reproducibility. (2) `SCALE_FRACTIONS` — each tiling texture's Scale as a fraction of part size (scratch 0.12, fine 0.02, fractal 0.15, cellular 0.08, spots 0.06), so a 40 mm part reproduces roughly the old good values (scratch → ~4.8 mm, matches the old 5.0) and auto-adapts to any part; replaces the hardcoded BASE absolutes (kept only as legacy jitter fallbacks). (3) every tiling builder now sets its Scale explicitly (incl. the **Spots-Scale fix** as a separate `set_display` call, not the collision-prone list) and calls `set_center_on_part(node)` — a type-2 enum set to "Part" (string then int 1/0, read-back pattern like `set_blend_mode`). (4) Spots Distortion (~0.4 + part-relative Distortion Scale) so pits read organic, not round. All new APIs/params UNPROBED → getattr/find_param guarded, always builds. Load-safe (19/19). AB04 kept on disk pending recycle. Earlier same day: `AB03 → AB04`: four new shader FAMILIES added as data, not new texture layers — (1) schema `extra` 6th tuple element on MATERIALS rows carrying `family` + family params (default `{}` = "opaque", every AB03 row untouched), threaded through sample_spec → spec['base']['extra'] → validate_spec → build_material with a `meta.family` echo; (2) four families — metal_aniso (Aluminum/Steel anisotropic: Metal + anisotropy + stable per-build brush angle), dielectric glass shipping BOTH clear and frosted on `SHADER_TYPE_DIELECTRIC` (fallbacks GLASS→SOLID_GLASS→Plastic, ior ~1.51, frosted drives base roughness high), thinfilm (oil-slick + anodised iridescent) on `SHADER_TYPE_THIN_FILM` (fallback Metal); (3) `apply_family_params` sets IOR/refraction-roughness/anisotropy+angle/film-thickness+IOR defensively and `resolve_shader` gained per-family fallback chains; (4) `FAMILY_ALLOWED_LAYERS` per-family wear-layer gating intersects chosen features before building the buses so clear glass never gets grime/pitting — glass and thin film drop spots/cellular/occlusion-grime/colour-gradient, logging each drop. ALL new constants + param names UNPROBED → getattr/find_param guarded, always builds. Load-safe (20/20). AB03 kept on disk pending recycle. Earlier same day: `AB02 → AB03`: three CONFIRMED bugfixes against a real AB02 DEBUG run — (1) roughness-bus composite now builds instead of always single-fallback, because `_composite_inputs` was filtering the Composite's Source/Background by `PT_COLOR` (type 13, a colour VALUE) when they are actually type-14 CONNECTION inputs — now matched by name with no type filter; (2) anti-repetition placement now jitters the REAL per-node scalar params from the dump (scale/noise_scale/level_scale/magnitude/seed/distortion/radius/shape_1..3/bias_x-y-z) instead of guessing offset/rotation names that don't exist — the type-12 Texture Transform matrix is left unset on purpose; (3) auto-apply walks the scene tree recursively via `getChildren()` and prints a candidate-kind histogram, so it reaches geometry instead of only cameras — CONFIRM-AT-RENDER. AB02 kept on disk pending recycle. Earlier same day: `AB01 → AB02` (find_param exact-match routing fix + Finish axis + Scratches Scale + seeded placement). Earlier: retired the NGS scripts — `1_HLP_MAT_GENERATOR` is now the single source of truth; made it KeyShot-load-safe + fixed the empty-dialog-default crash; filed the generator into stage 1, synthesised the AUTO/PRO launcher research, added masked-wear + launcher backlog)

Running at-a-glance inventory + priority backlog. Update this file, don't
replace it, as scripts move status.

## Current inventory

| Stage | Area | Script | Status | Notes |
|---|---|---|---|---|
| 0 | VAL | `0_VAL_STEP_PREFLIGHT_AA01.py` | ✅ current | STEP-file preflight (not a lux script) — unit/scale declaration check, filename hygiene, duplicate base names, best-effort material-name hints. Forum-grounded: unit/scale mismatch is the #1 reported KeyShot import complaint |
| 0 | VAL | `0_VAL_IMPORT_HEALTH_AA01.py` | ✅ current | Trial-import geometry/appearance health check — bounding-box sanity, object-count sanity, Studio/camera presence, before committing a folder to the full batch pass |
| 0 | VAL | `0_VAL_ORIENTATION_CHECK_AA01.py` | ✅ current | Orientation/up-axis sanity check — flags statistical footprint-ratio outliers across a batch. Explicitly a review-flagging heuristic, not a fixer |
| 0 | CHK | `0_CHK_MATGRAPH_PROBE_AA01.py` | 🔬 diagnostic | One-off material-graph capability probe (not a pipeline stage) — runs the 13 probes from `MATERIAL_DIVERSITY_DESIGN.md` §8, PASS/FAIL + param dumps, verifies edges landed. Run in the console, paste output for MDD-4B7A9F Rev 2, delete the `MATGRAPH_PROBE` material after |
| 0 | VAL | `0_VAL_LOAD_SAFETY_AA01.py` | 🛡️ dev guard | Dev/CI check (runs in normal Python, not KeyShot) — AST + ASCII scan that fails any KeyShot script with an f-string or non-ASCII char (the load-blocker). Run `python 0_VAL_LOAD_SAFETY_AA01.py` before committing; 18/18 pass |
| 1 | HLP | `1_HLP_MAT_PREFLIGHT_AA01.py` | ✅ current | Material-template coverage QC — renamed from `batch_material_preflight.py` |
| 1 | HLP | `1_HLP_MAT_LOOKUP_AA01.py` | ✅ current | Creo → KeyShot material-name lookup table; applies templates per object. Per-object apply method is unconfirmed on your build — probes candidates, reports which worked |
| 1 | HLP | `1_HLP_MAT_GENERATOR_AB05.py` | 🧪 validating | **Part-size-aware texture scaling** on AB04's four-families rev. AB04 and earlier were NOT part-size aware: no bounding-box query anywhere, and `Center On` (texture_space) was never set → KeyShot defaulted to *Center On: Model*, mapping every texture to the whole ~6700-unit MODEL (feature sizes wildly wrong on a ~40 mm part); texture Scales were hardcoded absolutes; and **Spots' own tiling Scale was never set** (the `["radius","size","scale"]` list matched Radius first → ~6700 default → giant blobs). AB05: (1) **`resolve_part_size(opts, name_filter)`** — entered `part_size_mm` (> 0) > `measure_part_size` (walks scene geometry via the recursive `getChildren()` collection, tries UNPROBED bbox APIs getWorldBounds/getBounds/getBoundingBox for the max extent) > `DEFAULT_PART_SIZE_MM = 50`; threaded into `spec['scale']` (`part_size_mm` + `source` + `fractions` + `resolved`). (2) **`SCALE_FRACTIONS`** — each tiling Scale as a fraction of part size (scratch 0.12 → ~4.8 mm on 40 mm, matches old 5.0; fine 0.02; fractal 0.15; cellular 0.08; spots 0.06), replacing the hardcoded absolutes and auto-adapting to any part. (3) **Scale on every node + Center On: Part** — every tiling builder (fine noise, scratches, spots, fractal, cellular) sets its Scale explicitly (incl. the Spots-Scale fix as a separate call) and `set_center_on_part` sets the type-2 enum to "Part" (string then int 1/0, read-back pattern). (4) **Spots distortion** (~0.4 + part-relative distortion scale). All new APIs/params UNPROBED → getattr/find_param guarded, always builds. Load-safe (19/19). Preserves every AB01–AB04 fix. Supersedes AB04 once render-confirmed |
| 1 | HLP | `1_HLP_MAT_GENERATOR_AB04.py` | ⤴️ superseded | Prior candidate — **four new shader FAMILIES** added as data (not new texture layers), on AB03's three-confirmed-bugfixes rev. (1) **Schema `extra`** — MATERIALS rows gain an optional 6th tuple element, a dict carrying `family` + family params; default `{}` = family "opaque", so every AB03 5-element row is untouched. Threaded through `sample_spec` → `spec['base']['extra']` (a COPY, never the module row) → `validate_spec` (numeric family params clamped) → `build_material`, with a `meta.family` echo. `material_family(extra)` returns the family string. (2) **Families** — *metal_aniso* ("Aluminum (anisotropic brushed)", "Steel (anisotropic)": Metal + anisotropy + a stable per-build brush angle from the placement RNG); *dielectric* glass ships BOTH "Glass (clear)" and "Glass (frosted)" on `SHADER_TYPE_DIELECTRIC` (fallbacks GLASS→SOLID_GLASS→Plastic), ior ~1.51, frosted drives base roughness high (+ a refraction-roughness param if present); *thinfilm* ("Thin Film (oil slick)", "Anodised (iridescent)") on `SHADER_TYPE_THIN_FILM` (fallback Metal), film thickness (nm) + film ior. (3) **Family-aware base setup** — `apply_family_params(graph, base_node, extra, rng)` applies the shader params defensively after colour+roughness (find_param-guarded, skip-with-warn); `resolve_shader` gained per-family fallback chains for the new constants. (4) **Per-family wear-layer gating** — `FAMILY_ALLOWED_LAYERS` intersects the chosen features with each family's allowed set BEFORE building the buses, logging each dropped layer; dielectric + thinfilm drop spots/cellular/occlusion-grime/colour-gradient (glass never gets grime/pitting). ALL new constants + param names UNPROBED → getattr/find_param guarded, always builds. Load-safe. Preserves every AB01/AB02/AB03/AA02 fix. Superseded by AB05 (part-size-aware scaling); kept on disk until AB05 is render-confirmed, then recycled (operator only) |
| 1 | HLP | `1_HLP_MAT_GENERATOR_AB03.py` | ⤴️ superseded | Prior candidate — three CONFIRMED bugfixes against a real AB02 DEBUG run (ground-truth node params): (1) roughness-bus composite input-type fix (`_composite_inputs` was filtering Source/Background by `PT_COLOR`/type-13 when they are type-14 connection inputs → always single-driver; now matched by name), (2) real per-node placement jitter (was guessing non-existent offset/rotation params; type-12 Texture Transform matrix left unset), (3) recursive `getChildren()` auto-apply + candidate-kind histogram (CONFIRM-AT-RENDER). Superseded by AB04 (the families rev); kept on disk until AB04 is render-confirmed, then recycled (operator only) |
| 1 | HLP | `1_HLP_MAT_GENERATOR_AB02.py` | ⤴️ superseded | Earlier candidate — find_param exact-match routing fix + Finish axis + Scratches Scale + seeded placement, on AB01's spec-refactor. Kept on disk pending recycle (operator only) |
| 1 | HLP | `1_HLP_MAT_GENERATOR_AB01.py` | ⤴️ superseded | Earlier candidate — spec-refactor + multi-source roughness blending. Kept on disk pending recycle (operator only) |
| 1 | HLP | `1_HLP_MAT_GENERATOR_AA02.py` | ♻️ fallback | Procedural material *variant* generator — toggleable feature layers (noise/scratches/spots/cellular/edges/roughness/colour) onto a metal/plastic base at a wear level, auto-named `MAT-<TYPE>-<WEAR>-<hex>`. Loud bump layers capped + `1/√n` damped. Now includes the **masked-wear base** (opt-in scratches→edges via Curvature, spots→cavities via Occlusion, composited through Color Composite; degrades to unmasked on any wiring failure). Made **KeyShot-load-safe** 2026-07-15 (f-string-free + ASCII-only — the old rev wouldn't load). Cellular + Colour Gradient are getattr-guarded experimental layers; mask input-slot names are probe-and-confirm on first real run |
| 2a | BAT | `2a_BAT_STD_VIEW_AA01.py` | ✅ current | Studios-first multi-view batch |
| 2a | BAT | `2a_BAT_TURNTABLE_AA01.py` | ✅ current | Studios-first turntable batch — kept as-is; "fade between angles" request built as a separate `3_` script instead (see backlog) |
| 2b | ANI | `2b_ANI_HERO_REVEAL_AA01.py` | ✅ current | Zoom+crane reveal; ground-setter & `getAnimationInfo()` shape still unconfirmed on your build |
| 2b | ANI | `2b_ANI_CUTAWAY_REVEAL_AA01.py` | ✅ current | Cutaway/cross-section reveal — clip plane sweep through an assembly, camera static. Clipping-plane & bounding-box APIs unconfirmed on your build — probes candidates, degrades to unclipped with a warning |
| 2b | ANI | `2b_ANI_ASSEMBLY_PROCEDURAL_AA01.py` | ✅ current | Procedural assembly reveal — 4 modes (scatter_settle, staggered_build, spiral_converge, ghost_fade), camera static, parts converge via relative transform deltas. Per-node transform composition, bounding-box sizing, and opacity control all unconfirmed — probes candidates, degrades gracefully. Sub-assembly build is a follow-on, not implemented |
| 3 | PRC | `3_PRC_CONTACT_SHEET_AA01.py` | ✅ current | Standalone HTML viewer (not a lux script) — renamed from `contact_sheet.py` |
| 3 | PRC | `3_PRC_FADE_REEL_AA01.py` | ✅ current | Multi-angle crossfade reel from batch stills — pure CSS `@keyframes`, no ffmpeg/dependencies |
| 4 | CHK | `4_CHK_AUDIT_AA01.py` | ✅ current | Naming-compliance + render-completeness audit — first script in stage 4 |
| 2a | BAT | `archive/batch_turntable.py` | 🗑️ archived | Raw-camera-only predecessor of `2a_BAT_TURNTABLE_AA01.py` — moved to `scripts/archive/`, out of the active pipeline |
| 2a | BAT | `archive/batch_import_and_render_all_views.py` | 🗑️ archived | Raw-camera-only predecessor of `2a_BAT_STD_VIEW_AA01.py` — moved to `scripts/archive/`, out of the active pipeline |
| 2b | ANI | `2ANI_SCATTERANIMATION_AQ.py` | ❓ exists elsewhere | Scatter tech — reference for the planned particle script |

_NGS scripts retired 2026-07-15: the whole `0_NGS_*` deploy on the KeyShot machine is being dropped. `0_NGS_PROC_MATS.py` (the deployed material generator) is superseded by `1_HLP_MAT_GENERATOR` here, which is now the single source of truth — deploy from this repo, not from the old NGS copies._

## Planned / backlog (priority order)

| Pri | Stage | Idea | Status | Depends on |
|---|---|---|---|---|
| P1 | 2b | Hand-staged one-off reveal (particles, rain) | Design | — |
| P1 | 2b | Assembly reveal — Creo-driven (animate an authored explode state) | Not started | Verify explode data survives plugin import |
| P1 | 2b | Assembly reveal — procedural modes (scatter/settle, staggered build, spiral converge, ghost fade-in) | ✅ done — `2b_ANI_ASSEMBLY_PROCEDURAL_AA01.py` (sub-assembly build split out as a follow-on, see below) | — |
| P1 | 2b | Cutaway / cross-section reveal | ✅ done — `2b_ANI_CUTAWAY_REVEAL_AA01.py` | — |
| P1 | 1 | Material-name lookup table (Creo → KeyShot) | ✅ done — `1_HLP_MAT_LOOKUP_AA01.py` | — |
| P2 | 3 | Multi-angle fade reel (crossfade between Studios/cameras) | ✅ done — `3_PRC_FADE_REEL_AA01.py` | Consumes stills from `2a_BAT_TURNTABLE`/`2a_BAT_STD_VIEW` |
| P2 | 3 | BOM-driven manifest (opt-in) | Not started | Research Q3 — BOM access method |
| P2 | 2b | Dynamic BOM callouts on assembly renders | Not started | BOM-driven manifest, above |
| P1 | 0 | **Material-graph probe pack** `0_CHK_MATGRAPH_PROBE` — the 13 probes in `MATERIAL_DIVERSITY_DESIGN.md` §8 (label slot, texture-into-mask, displace, new base/texture node params, image-map paths, blend-mode format, multi-material…) to convert design assumptions into ground truth. Phase 0 of the diversity roadmap | ✅ built — run it | Ship as `0_CHK_MATGRAPH_PROBE_AA01.py`; run in the console, paste output for MDD-4B7A9F Rev 2 |
| P1 | 1 | **Material generator spec-refactor + routing/finish/placement + confirmed bugfixes + families + part-size scaling (AB01→…→AB05)** — MaterialSpec + 3 buses + multi-source roughness blending (Lighten), then the find_param exact-match fix, Finish preset axis + Scratches Scale, and seeded anti-repetition placement (AB02), then three confirmed bugfixes against a real DEBUG run: roughness-composite input-type fix, real per-node placement jitter, geometry-reaching recursive auto-apply (AB03), then four new shader families (anisotropic metal, dielectric glass clear+frosted, thin film) via a per-row `extra` dict + family-aware base-param pass + per-family wear-layer gating (AB04, per `AB04_FAMILIES_SPEC.md`), then **part-size-aware texture scaling** — `resolve_part_size` (entered/measured/default) + `SCALE_FRACTIONS` (part-relative Scales) + `set_center_on_part` (Center On: Part) + the Spots-Scale-never-set fix + Spots distortion (AB05). Then more palettes → finishes → weathering engine → batch "lottery" | 🧪 built, validating | Now `1_HLP_MAT_GENERATOR_AB05.py` (AB04 superseded, kept on disk); render-confirm then retire AA02 + recycle AB04/AB03/AB02/AB01. Later phases build on it, per `MATERIAL_DIVERSITY_DESIGN.md` §9 |
| P1 | 1 | Masked / targeted wear in the material generator — edge-only scratches (Curvature mask), cavity grime (Occlusion mask), occasional fingerprints (image + noise-gated roughness) via a per-layer `masked()` Color-Composite stage | Research done | Spec: `scripts/research/MASKED_WEAR_RESEARCH.md` (MWR-9C4E21). Node ids confirmed scriptable; no label API so mask the effect, not a label. Recipe A (edge scratches) is the first build |
| P1 | 2a/2b | **Render/animation Phase 1 — load-safety + reliability** (RAR-6E1F3B §6) | ✅ done | `18:54` — all 5 scripts load + none can silently render nothing: 2b load-safety, `setActiveStudio`-first in all 5, verify-render-wrote + verify-frames-before-encode, per-part import `try`, killed the 2a headless `input()` hang, env `%360`, + a new `0_VAL_LOAD_SAFETY` guard (18/18 pass). In-place on AA01; AB01 letter-bump rides with Phase 2. Next: **Phase 2** = L3 `ks_shared.py` + RenderSpec presets + Manifest v2 |
| P2 | 2a/2b | **Render/animation Phases 2–6** (RAR-6E1F3B): L3 `ks_shared.py` shared module; RenderSpec preset system (draft/review/hero/comp/print/turntable, EXR+passes, denoise, GPU engine); assembly **motion-inverted** fix + `getTransform` drift check; **cutaway redesign** (clip-plane API doesn't exist → `SHADER_TYPE_CUTAWAY` cutter-solid sweep); `renderAnimation` direct path; RUN_ALL launcher integration | Design | 13-probe pack (RAR §6) to run first, like MDD's |
| P2 | 1 | Studio/camera-rig template library | Not started | — |
| P2 | 0 | STEP-file preflight (units, filename hygiene, material-name hints) | ✅ done — `0_VAL_STEP_PREFLIGHT_AA01.py` | Forum-grounded: STEP unit/scale mismatch is the #1 reported KeyShot import complaint (see RESEARCH_CREO_KEYSHOT.md) |
| P2 | 0 | Trial-import geometry/appearance health check | ✅ done — `0_VAL_IMPORT_HEALTH_AA01.py` | This is the original "pre-import geometry/appearance validator" item, now split out and grounded in specific forum reports (missing assembly components, tessellation gaps, failed imports) |
| P2 | 0 | Orientation / up-axis sanity check | ✅ done — `0_VAL_ORIENTATION_CHECK_AA01.py` | Forum-grounded: KeyShot is always Y-up, source CAD varies, easy to get wrong unattended |
| P2 | 4 | Naming-compliance + render-completeness audit | ✅ done — `4_CHK_AUDIT_AA01.py` | — |
| P3 | — | Archive superseded batch scripts | ✅ done — moved to `scripts/archive/` | — |
| P2 | 2b | Assembly reveal — sub-assembly build (grouped convergence) | Not started | Extension of `2b_ANI_ASSEMBLY_PROCEDURAL_AA01.py`; extension point noted in that script's `get_parts()`/`schedule_staggered_windows()` |
| P1 | — | Shared module (L3) — de-duplicate Studio resolution, manifest logging, `centerAndFit`+padding, units/bbox classify out of the ~4 scripts that copy it | Design | The unblocker for both the launcher and the scale classifier; extract before writing either (see synthesis §4) |
| P1 | — | `RUN_ALL.py` launcher (L1) — one spine, two doors (AUTO headless / PRO dialog): import → classify tier → preflight hard-gate → stage router → contact sheet | Design | Shared module (above). AUTO/PRO/EXPERT design in `RENDER_PIPELINE_ARCHITECTURE.md` |
| P2 | — | `watch.py` orchestrator (L0, out-of-KeyShot) — hot-folder watch on `01_FOR_PROCESSING/*`, resolve mode from subfolder, move-on-success lifecycle | Design | `RUN_ALL.py` first |
| P2 | 0 | 5-tier bbox-diagonal scale classification (Micro→Extra-large, ~35,000:1 range) — per-tier overrides dict merged onto base options | Design | Needs a calibration pass over real parts for breakpoints; `getSceneInfo()` unit-scale + `getBoundingBox(world=True)` both confirmed |

See `RESEARCH_CREO_KEYSHOT.md` for the thinking behind the backlog — its
2026-07-15 synthesis section covers the launcher layer, and the two source
docs in `scripts/research/` (RPA-7B2E4D, RPR-3F9C1A) have the full detail.
